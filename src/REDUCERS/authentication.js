const authenticationReducer=(state,action)=>{

    switch (action.type) {
        case "LOGIN":
            
            break;
    
            case "SIGNUP":
            
                break;

                case "LOGOUT":
            
            break;   

        default:
            break;
    }


}